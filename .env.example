GROQ_API_KEY=
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=
DAILY_TOKEN_BUDGET=8000
CRON_SECRET=
CJ_SID_PREFIX=recommendmeafragrance
# CJ Product Feed GraphQL API (https://ads.api.cj.com/query) - replaces the
# old static-CSV-URL feed approach, which CJ no longer offers for new setups.
CJ_API_TOKEN=
CJ_COMPANY_ID=
CJ_ADVERTISER_IDS=
CJ_PID=
# Google OAuth (console.cloud.google.com -> Credentials -> OAuth client ID,
# type Web application; register <origin>/api/auth/google/callback for both
# localhost:3000 and the production domain).
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
